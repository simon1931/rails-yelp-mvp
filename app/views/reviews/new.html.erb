<h1>Add a review</h1>
<%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::AUTHORIZED_RATING%>
    <%= f.button :submit, class: "btn btn-primary mt-3" %>
<% end %>
